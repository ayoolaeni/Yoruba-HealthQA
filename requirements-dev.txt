# Lightweight, CPU-only subset of requirements.txt used to develop and unit-test
# the parts of the pipeline that do not require a GPU or model weights:
# src/data/yoruba_text.py, src/serve/guardrails.py, src/eval/metrics.py (chrF++/kappa/alpha
# portions), schema validation, and dataset splitting/filtering logic.
#
# Install this on a laptop with no GPU to run `pytest` and the Phase 1 dataset
# scripts. For training (Phase 3/4) and full evaluation (bert-score, COMET),
# install requirements.txt on a CUDA machine instead.
pyyaml==6.0.2
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
krippendorff==0.6.1
matplotlib==3.9.2
sacrebleu==2.4.3
rouge-score==0.1.2
tqdm==4.66.5
python-dotenv==1.0.1
rich==13.8.1
openpyxl==3.1.5
tabulate==0.9.0
pytest==8.3.3
requests==2.32.3
beautifulsoup4==4.12.3

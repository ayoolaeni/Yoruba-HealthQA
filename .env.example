# Copy to .env and fill in what you use. Never commit the real .env.

# Required only for scripts/02_build_qa.py --question-backend llm
# and scripts/10_evaluate.py's B4 (proprietary baseline) system.
ANTHROPIC_API_KEY=

# Required only if scripts/10_evaluate.py's B4 system is configured with a
# different provider in configs/eval.yaml (not implemented beyond Anthropic
# yet -- see scripts/10_evaluate.py:generate_proprietary).
# OPENAI_API_KEY=

# app/app.py model configuration (Phase 8 prototype).
YHQA_BASE_MODEL=
YHQA_ADAPTER_PATH=

# Optional: raises Hugging Face Hub download rate limits.
HF_TOKEN=
